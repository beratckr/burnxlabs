---
import Layout from '../../layouts/Layout.astro';
import { useTranslations, getLangFromUrl } from '../../i18n/utils';
import { languages } from '../../i18n/config';

export function getStaticPaths() {
    return Object.keys(languages).map((lang) => ({
        params: { lang }
    }));
}

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Blog posts data (would normally come from a CMS or markdown files)
const blogPosts = [
    {
        id: 1,
        slug: 'why-beginners-fail-at-fitness',
        title: lang === 'tr' ? 'Yeni Başlayanlar Fitness\'ta Neden Başarısız Olur (ve BurnX Bunu Nasıl Çözer)' : 'Why Beginners Fail at Fitness (and How BurnX Solves It)',
        excerpt: lang === 'tr' ? 
            'Fitness uygulamalarının %80\'i ilk ay içinde terk ediliyor. Sorun sizde değil - sistemde. BurnX\'in farklı yaklaşımını keşfedin.' :
            'Most fitness apps lose 80% of users in the first month. The problem isn\'t you - it\'s the system. Learn how BurnX is different.',
        author: lang === 'tr' ? 'Sarah Chen, CEO' : 'Sarah Chen, CEO',
        date: lang === 'tr' ? '15 Ocak 2025' : 'January 15, 2025',
        readTime: lang === 'tr' ? '5 dk okuma' : '5 min read',
        category: lang === 'tr' ? 'Fitness' : 'Fitness',
        image: '/api/placeholder/800/400'
    },
    {
        id: 2,
        slug: 'science-of-habit-formation',
        title: lang === 'tr' ? 'Egzersiz Alışkanlığının Arkasındaki Bilim' : 'The Science Behind Exercise Habit Formation',
        excerpt: lang === 'tr' ? 
            'Araştırmalar yeni bir alışkanlık oluşturmanın ortalama 66 gün sürdüğünü gösteriyor. BurnX\'in yapay zekası bu süreçte size nasıl yardımcı oluyor?' :
            'Research shows it takes an average of 66 days to form a new habit. Here\'s how BurnX\'s AI helps you through this journey.',
        author: lang === 'tr' ? 'Dr. Emily Rodriguez' : 'Dr. Emily Rodriguez',
        date: lang === 'tr' ? '10 Ocak 2025' : 'January 10, 2025',
        readTime: lang === 'tr' ? '7 dk okuma' : '7 min read',
        category: lang === 'tr' ? 'Bilim' : 'Science',
        image: '/api/placeholder/800/400'
    },
    {
        id: 3,
        slug: 'home-workouts-guide',
        title: lang === 'tr' ? 'Evde Egzersiz İçin Eksiksiz Rehber' : 'The Complete Guide to Home Workouts',
        excerpt: lang === 'tr' ? 
            'Spor salonuna gitmeye gerek yok. Evinizin konforunda fit olmanız için ihtiyacınız olan her şey burada.' :
            'No gym? No problem. Everything you need to know about getting fit from the comfort of your home.',
        author: lang === 'tr' ? 'Marcus Johnson, CTO' : 'Marcus Johnson, CTO',
        date: lang === 'tr' ? '5 Ocak 2025' : 'January 5, 2025',
        readTime: lang === 'tr' ? '10 dk okuma' : '10 min read',
        category: lang === 'tr' ? 'Antrenman' : 'Workouts',
        image: '/api/placeholder/800/400'
    }
];
---

<Layout title={t('blog.title')}>
    <!-- Hero Section -->
    <section class="pt-16 pb-20 px-6 bg-gradient-to-b from-gray-50 to-white">
        <div class="max-w-4xl mx-auto text-center">
            <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                {t('blog.title')}
            </h1>
            <p class="text-xl text-gray-600 leading-relaxed">
                {t('blog.subtitle')}
            </p>
        </div>
    </section>

    <!-- Blog Posts Grid -->
    <section class="py-20 px-6">
        <div class="max-w-6xl mx-auto">
            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                {blogPosts.map((post) => (
                    <article class="bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-md transition-shadow">
                        <!-- Image -->
                        <div class="aspect-video bg-gradient-to-br from-blue-100 to-pink-100"></div>
                        
                        <!-- Content -->
                        <div class="p-6">
                            <!-- Category & Date -->
                            <div class="flex items-center justify-between text-sm text-gray-500 mb-3">
                                <span class="bg-gray-100 px-3 py-1 rounded-full">{post.category}</span>
                                <span>{post.readTime}</span>
                            </div>
                            
                            <!-- Title -->
                            <h2 class="text-xl font-bold text-gray-900 mb-3 line-clamp-2">
                                <a href={`/${lang}/blog/${post.slug}`} class="hover:text-blue-600 transition-colors">
                                    {post.title}
                                </a>
                            </h2>
                            
                            <!-- Excerpt -->
                            <p class="text-gray-600 mb-4 line-clamp-3">
                                {post.excerpt}
                            </p>
                            
                            <!-- Author & Date -->
                            <div class="flex items-center justify-between">
                                <div class="text-sm">
                                    <p class="text-gray-900 font-medium">{post.author}</p>
                                    <p class="text-gray-500">{post.date}</p>
                                </div>
                                <a 
                                    href={`/${lang}/blog/${post.slug}`}
                                    class="text-blue-600 hover:text-blue-700 font-medium text-sm flex items-center"
                                >
                                    {t('blog.readMore')}
                                    <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                    </svg>
                                </a>
                            </div>
                        </div>
                    </article>
                ))}
            </div>
            
            <!-- Coming Soon Message -->
            <div class="mt-16 text-center">
                <div class="bg-blue-50 rounded-2xl p-8 max-w-2xl mx-auto">
                    <h3 class="text-xl font-semibold text-gray-900 mb-3">
                        {lang === 'tr' ? 'Daha fazla içerik yolda!' : 'More content coming soon!'}
                    </h3>
                    <p class="text-gray-600 mb-6">
                        {lang === 'tr' ? 
                            'Fitness, beslenme ve sağlıklı yaşam hakkında düzenli içerikler yayınlayacağız. Yeni yazılardan haberdar olmak için bekleme listemize katılın.' :
                            'We\'ll be publishing regular content about fitness, nutrition, and wellness. Join our waitlist to be notified of new posts.'}
                    </p>
                    <a href={`/${lang}/#waitlist`} class="inline-block bg-gray-900 text-white px-6 py-3 rounded-lg font-medium hover:bg-gray-800 transition-colors">
                        {t('hero.cta')}
                    </a>
                </div>
            </div>
        </div>
    </section>
</Layout>

<style>
    .line-clamp-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    
    .line-clamp-3 {
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
</style>